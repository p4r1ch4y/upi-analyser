<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- CRITICAL: No INTERNET permission. This is the entire positioning. -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->
    
    <!-- Core permissions for notification capture -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Boot receiver to re-arm service -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- Battery optimization exemption (will be requested with user consent) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Storage Access Framework for backups (no INTERNET needed) -->
    <!-- No permission needed - SAF handles it -->

    <!--
      Named UPI apps only - never QUERY_ALL_PACKAGES.

      Note this list does not gate notification capture: a bound
      NotificationListenerService is delivered every app's notifications
      regardless. It exists so the app can resolve these packages by name (for
      icons and deep links) without asking to see the user's whole app list.

      Kept in sync with UpiNotificationListener.targetPackages, which is what
      actually decides whose notifications get parsed.
    -->
    <queries>
        <!--
          Package visibility filtering hides every other app from resolveActivity
          unless it is declared here. Without this, the check for a mail client
          returned "none installed" on a phone with Gmail on the home screen.
        -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <package android:name="com.google.android.apps.nbu.paisa.user" />
        <package android:name="com.phonepe.app" />
        <package android:name="net.one97.paytm" />
        <package android:name="in.org.npci.upiapp" />
        <package android:name="in.amazon.mShop.android.shopping" />
        <package android:name="com.dreamplug.androidapp" />
        <package android:name="money.super.payments" />
        <package android:name="in.slice.android" />
        <package android:name="com.jupiter.consumer" />
        <package android:name="com.epifi.paisa" />
        <package android:name="com.naviapp" />
        <package android:name="com.whatsapp" />
        <package android:name="com.mobikwik_new" />
        <package android:name="com.freecharge.android" />
        <package android:name="com.csam.icici.bank.imobile" />
        <package android:name="com.snapwork.hdfc" />
        <package android:name="com.sbi.upi" />
        <package android:name="com.axis.mobile" />
        <package android:name="com.msf.kbank.mobile" />
    </queries>

    <application
        android:name=".SpendLensApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpendLens"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">
        
        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SpendLens"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Notification Listener Service -->
        <service
            android:name=".service.UpiNotificationListener"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Foreground Service -->
        <service
            android:name=".service.TransactionCaptureService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
          Accepts a payment receipt shared from a UPI app. No permission of any
          kind: the user pushes the text here, the app never reaches for it.
        -->
        <activity
            android:name=".service.ShareReceiverActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:label="@string/share_target_label"
            android:theme="@style/Theme.SpendLens.Invisible">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!--
              Images too. Most UPI apps share a screenshot rather than text, and
              without this SpendLens never appears in their share sheet at all.
              The screenshot is not read - it opens manual entry with the receipt
              shown inside the form, and the date taken from the screenshot.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <!--
              Some gallery and UPI apps send a single screenshot as SEND_MULTIPLE
              with one item. Without this filter SpendLens is simply missing from
              their share sheet, which reads as the app not supporting sharing at
              all rather than as a mismatch of intent actions.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!--
          Quick Settings tile: one tap from the shade to note what the payment you
          just made was actually for, while you still remember.
        -->
        <service
            android:name=".service.QuickNoteTile"
            android:exported="true"
            android:icon="@drawable/ic_notification"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Boot Receiver -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
